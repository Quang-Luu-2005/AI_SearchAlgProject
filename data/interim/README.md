# Interim data

Đầu ra tạm của bước clean, simplify topology và map-match. Mọi file ở đây phải
có thể tạo lại từ `data/raw/` bằng script; Git chỉ giữ README này.

- `osm_thu_duc_v1.0.0/`: Các file topology làm sạch trung gian (`interim_nodes.csv`, `interim_edges.csv`) sinh tự động khi chạy `scripts/data/build_osm_dataset.py`.


